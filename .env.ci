# CI Environment Configuration for GitHub Actions
# This file contains environment variables for running tests in CI

# Database
DATABASE_URL=postgresql+psycopg://raguser:changeme@localhost:5432/ragdb

# Redis
REDIS_URL=redis://:changeme@localhost:6379/0

# MinIO
MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=documents
MINIO_SECURE=false

# JWT
JWT_SECRET_KEY=test-secret-key-for-ci-do-not-use-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRATION_MINUTES=30

# OpenAI (placeholder for tests)
OPENAI_API_KEY=test-openai-key

# Application
APP_NAME=RAG RBAC CI
DEBUG=true
HOST=0.0.0.0
PORT=8000